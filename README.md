# Purawepa

Static site, no build step. Serve locally with either:

```
npx serve .
```

or

```
python3 -m http.server 8080
```

Then open the printed local URL. The hero's scroll-zoom sequence needs a real scroll gesture (or mouse-wheel emulation) to see — it won't do anything on a static screenshot of the top of the page.
